---
const { data } = Astro.props;
---
<div class="grid grid-cols-3 gap-4 mb-4">
  {data.map((item) => (
    <div class={`p-2 rounded ${item.highlighted ? 'bg-yellow-300' : 'bg-gray-200'}`}>
      {item.title}
    </div>
  ))}
</div>